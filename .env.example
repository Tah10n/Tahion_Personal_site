# Project X-Ray can optionally call Repo Analyzer Service before falling back
# to browser-static mode. Keep this value public; never expose API keys here.
#
# VITE_XRAY_BACKEND_URL=http://127.0.0.1:3000
